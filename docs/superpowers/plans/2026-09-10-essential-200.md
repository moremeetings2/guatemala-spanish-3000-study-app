# Essential 200 implementation plan

**Approved goal:** Add a separate Essential 200 deck inside Hablavos, push the completed app to GitHub, then create and email a matching Google Sheet to John.Moore@itroundtableonline.com as the final task.

**Architecture:** A curated manifest selects 190 existing Main 3000 card IDs and adds ten missing basics. Deck membership shares the original cards and progress, with 3,609 unique built-in cards. The manifest owns the reviewed English meanings and Spanish/English example sentences used in both app and spreadsheet. The existing You → deck → Browse → Study these / Quiz these flow provides entry points. Cache the manifest for offline use.

**Tech stack:** Static JavaScript PWA, JSON content, Playwright Chromium/WebKit, Artifact Tool spreadsheet export, Google Drive and Gmail connectors.

- [x] Extend app integration tests for the 200-card deck, exact membership, sentences, Study/Quiz UI, shared progress, and resume after reload; run headed and observe missing-deck failure.
- [x] Add `data/essential_200.json` with 200 unique stable IDs grouped by everyday speaking use; integrate its membership in `app.js`, deck counts and study label; cache it in `sw.js` and bump the cache version.
- [x] Verify every selected word and sentence, including English translations. Run the new tests and all Playwright tests on Chromium and WebKit in headed mode.
- [x] Log in with the real admin account from `.env` in a headed browser. Exercise the new deck, sentence audio, quiz, progress synchronization, resume, lexicon, settings and admin CRUD; delete temporary words and neutralize touched progress.
- [ ] Update README and AGENTS documentation. Review the diff, commit only task files with detailed verification evidence, push main, wait for Pages deployment and verify live content.
- [ ] As the final task, generate a styled XLSX from the exact app JSON sources, import as a native Google Sheet, verify all 200 rows, share with the requested recipient and email the verified link. Report the Sheet link and app location.

## Verification evidence

- Initial missing-deck tests failed as expected; quiz-choice isolation tests also failed before implementation.
- `npm run test:integration:headed -- --workers=4`: 124 passed across Chromium and WebKit (49.2 seconds).
- Dedicated-origin disconnect verifies offline cache fallback in both browsers; Playwright WebKit `setOffline` blocks service-worker responses, so it is not used for that check.
- Headed Chromium and WebKit logged into the real API admin account and exercised exact 200-row content, browse, study, example/speech invocation, new-card progress sync, reload resume, quiz answer/sync, lexicon, settings, and admin word create/edit/delete. Temporary words were deleted; touched progress was reset and verified.
- Git fetch initially encountered zero-byte macOS Finder Icon files inside `.git/refs`; those metadata files were preserved outside Git refs, and fetch then succeeded.
